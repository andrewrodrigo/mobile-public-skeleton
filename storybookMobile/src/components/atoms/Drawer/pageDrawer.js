import React from 'react';
import Button from '../Button'

const PageDrawer = (props) => {
    console.warn('PROPS DRAWER -->' , props)
    return (<Button onPress={()=>{}} title="Page Drawer"/>)
};

export default PageDrawer;
